# CODA_Export
Export Coda docs into markdown or html for offline use or new service data transfer

Input your API key into the code and follow the embedded instructions. Initial space selection is based on "starred" spaces due to the large number and complexity of being part of multiple spaces that may or may not have API access enabled.

Utilizes an async threading process to significantly improve the efficiency versus sequential API calls.

Saves files to script location.
